import React from "react";
import styles from "./cards.module.css";

const Cards = ({ id, name, images, type, repo, deploy }) => {
  return (
    <div className={styles.card}>
      
    </div>
  );
};

export default Cards;
